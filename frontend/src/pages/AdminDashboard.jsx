import React from 'react';

export default function AdminDashboard(){
  return (
    <div>
      <h1 className="text-2xl font-bold">Admin Dashboard</h1>
      <p className="mt-4">Use API routes to manage products and orders (requires admin JWT).</p>
    </div>
  );
}
